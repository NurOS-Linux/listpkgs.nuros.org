/* ============================================================================
   CSS MODULES - Global styles
   ============================================================================

   This file can be used if you prefer CSS modules over SCSS for certain components.
   Import individual files as needed in components:
   import styles from './component-name.module.css';

   Example usage:

   ```jsx
   <div className={styles.className}>content</div>
   ```

========================================================================== */
