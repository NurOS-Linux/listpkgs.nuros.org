name: Build Frontend

on:
  workflow_run:
    workflows: ["Update NurOS Package List"]
    types:
      - completed
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  check-lint:
    runs-on: ubuntu-latest
    outputs:
      lint-passed: ${{ steps.check.outputs.passed }}
    steps:
      - name: Check lint status
        id: check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          LINT_RESULT=$(gh run list \
            --repo ${{ github.repository }} \
            --workflow "Lint Frontend" \
            -s success \
            --limit 1 \
            --json conclusion \
            -q '.[0].conclusion // "unknown"')
          
          echo "Latest lint result: $LINT_RESULT"
          
          if [ "$LINT_RESULT" = "success" ]; then
            echo "passed=true" >> $GITHUB_OUTPUT
            echo "✅ Lint passed"
          else
            echo "passed=false" >> $GITHUB_OUTPUT
            echo "❌ Lint failed or not run"
          fi

  build:
    needs: check-lint
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: |
      (github.event_name == 'workflow_dispatch') ||
      ((github.event.workflow_run.conclusion == 'success') && (needs.check-lint.outputs.lint-passed == 'true'))
    defaults:
      run:
        working-directory: ./listpkgs.nuros.front-end

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Download repodata artifact
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "📥 Attempting to download latest repodata..."
          
          UPDATE_RUN=$(gh run list \
            --repo ${{ github.repository }} \
            --workflow "Update NurOS Package List" \
            -s success \
            --limit 1 \
            --json databaseId \
            -q '.[0].databaseId // ""')
          
          if [ -z "$UPDATE_RUN" ]; then
            echo "ℹ️  No update-list run found, using committed version"
            exit 0
          fi
          
          ARTIFACT=$(gh api \
            repos/${{ github.repository }}/actions/runs/$UPDATE_RUN/artifacts \
            -q '.artifacts[] | select(.name=="repodata-json") | .id // ""') 
          
          if [ -z "$ARTIFACT" ]; then
            echo "ℹ️  No repodata artifact found, using committed version"
            exit 0
          fi
          
          mkdir -p public
          
          echo "Downloading artifact..."
          gh api repos/${{ github.repository }}/actions/artifacts/$ARTIFACT/zip \
            -H "Accept: application/vnd.github.v3.raw" > /tmp/repodata.zip
          
          echo "Extracting..."
          unzip -o /tmp/repodata.zip -d public/
          rm /tmp/repodata.zip
          
          echo "✅ Downloaded latest repodata files"
          ls -lh public/repodata.json* 2>/dev/null || true

      - name: Verify source data exists
        run: |
          if [ ! -f "public/repodata.json" ]; then
            echo "❌ ERROR: repodata.json not found in public/"
            exit 1
          fi
          echo "✅ Source data verified"
          wc -l public/repodata.json

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          check-latest: true
          cache: 'pnpm'
          cache-dependency-path: '**/pnpm-lock.yaml'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build frontend
        run: |
          pnpm build
          echo "✅ Build complete"
          du -sh dist/
          ls -1 dist/ | head -10

      - name: Copy repodata to dist
        run: |
          if [ ! -f "dist/repodata.json" ]; then
            cp public/repodata.json dist/
            echo "ℹ️  Manually copied repodata.json to dist"
          else
            echo "✅ repodata.json already in dist"
          fi
          
          if [ -f "public/repodata.json.sha256" ]; then
            cp public/repodata.json.sha256 dist/
          fi

      - name: Verify build artifacts
        run: |
          if [ ! -f "dist/index.html" ]; then
            echo "❌ ERROR: index.html not found in dist/"
            exit 1
          fi
          if [ ! -f "dist/repodata.json" ]; then
            echo "❌ ERROR: repodata.json not found in dist/"
            exit 1
          fi
          echo "✅ All critical files present in dist/"
          ls -lh dist/index.html
          ls -lh dist/repodata.json

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: listpkgs.nuros.front-end/dist/
          retention-days: 7
          compression-level: 9
