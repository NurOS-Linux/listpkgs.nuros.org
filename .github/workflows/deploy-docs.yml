# ⚠️ DEPRECATED: This workflow has been replaced by sync-gitbook.yml
#
# Documentation is now hosted on GitBook instead of GitHub Pages.
# This file is kept for reference only and should not be used.
#
# New workflow: .github/workflows/sync-gitbook.yml
# Documentation: blog/GITBOOK_SETUP.md

name: ⚠️ DEPRECATED - Use sync-gitbook.yml Instead

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Show deprecation notice
        run: |
          echo "⚠️  This workflow is DEPRECATED"
          echo ""
          echo "Documentation has been migrated to GitBook."
          echo "Use 'sync-gitbook.yml' workflow instead."
          echo ""
          echo "For setup instructions, see:"
          echo "- blog/GITBOOK_SETUP.md"
          echo "- .github/workflows/sync-gitbook.yml"
          exit 1
